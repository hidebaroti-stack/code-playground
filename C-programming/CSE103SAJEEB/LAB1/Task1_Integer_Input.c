#include <stdio.h>

// Task 1: Write a C program that takes an integer input from the user and prints it back.
int main()
{
    int i;
    printf("Enter an integer: ");
    scanf("%d", &i);
    printf("You entered: %d\n", i);
    return 0;
}